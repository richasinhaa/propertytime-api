{% trans_default_domain 'FOSUserBundle' %}

<form id="changePasswordResetId" ng-submit="changePasswordReset('{{ path('fos_user_change_password') }}', 'POST')" {{ form_enctype(form) }} class="fos_user_change_password">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'change_password.submit'|trans }}" />
    </div>
</form>
