{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    <p>{{ 'profile.show.username'|trans }}: {{ user.name }}</p>
    <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
    <p> Phone : {{ user.phone }}</p>
</div>
